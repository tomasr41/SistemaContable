# Configuración de la base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/SistemaContableDB1
spring.datasource.username=postgres
spring.datasource.password=leod0b
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Configuración JWT
jwt.secret=miClaveSecretaSuperSeguraParaJWT2024SistemaContable
jwt.expiration=86400000

# Puerto del servidor
server.port=8080

# CORS
cors.allowed.origins=http://localhost:5173

# Logging
logging.level.com.sistemacontable=DEBUG
logging.level.org.springframework.security=DEBUG